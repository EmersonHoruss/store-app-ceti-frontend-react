export const config = {
    options: {
        // method: "",
        // body: {},
        header: {
            "Content-Type": "application/json"
        },
        // credentials: "omit"
    },
    baseUrl: "http://localhost:3000"
}